# Hedera Testnet Configuration
HEDERA_ACCOUNT_ID=0.0.YOUR_ACCOUNT_ID
HEDERA_PRIVATE_KEY=YOUR_PRIVATE_KEY
HEDERA_NETWORK=testnet
HEDERA_TOPIC_ID=0.0.YOUR_TOPIC_ID  # Auto-generated on first run, or set manually

# Reward Configuration
BASE_REWARD_HBAR=0.001  # Base reward amount in HBAR per MLAT contribution
ENABLE_REAL_TRANSFERS=false  # Set to 'true' to enable actual HBAR transfers (requires sufficient balance)

# Token Configuration (Optional - for future HTS token rewards)
REWARD_TOKEN_ID=0.0.YOUR_TOKEN_ID

# Server Configuration
PORT=3001
